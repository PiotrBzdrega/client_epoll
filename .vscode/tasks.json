{
	"version": "2.0.0",
	"tasks": [
		{
			"type": "cppbuild",
			"label": "C/C++: g++ build active file",
			"command": "C:\\msys64\\ucrt64\\bin\\g++.exe",//"/usr/bin/g++",
			"args": [
				// "-std=c++17",
				"-fdiagnostics-color=always",
				"-g",
				"${fileDirname}${pathSeparator}main.cpp",
				"${fileDirname}${pathSeparator}EndPoint.cpp",
				"${fileDirname}${pathSeparator}Epoll.cpp",
				"${fileDirname}${pathSeparator}TLS.cpp",
				"${fileDirname}${pathSeparator}IO.cpp",
				"-I${fileDirname}",
				"-I/usr${pathSeparator}include${pathSeparator}openssl", // OpenSSL include directory
				"-L${pathSeparator}usr${pathSeparator}lib", // General library directory
				"-lssl", // Link OpenSSL SSL library
				"-lcrypto", // Link OpenSSL Crypto library
				"-o",
				"${fileDirname}${pathSeparator}build${pathSeparator}client",
				
			],
			"options": {
				"cwd": "${fileDirname}"
			},
			"problemMatcher": [
				"$gcc"
			],
			"group": "build",
			"detail": "compiler: /usr/bin/g++"
		}
	]
}